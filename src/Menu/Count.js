import React from "react";

const Count = (props) => {
    return (
        <div>
            {props.days} days of coding since this portfolio was published
        </div>
    );
};

export default Count;
